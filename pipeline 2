# PL_Load_Oracle_To_Fabric — Authored Names & Descriptions

Ready to paste into each activity's **Description** field in Fabric. The **Rename** column tells you whether changing the activity's `name` is safe on its own, or whether other activities reference it by name (via `dependsOn` or an `@activity('...')` expression) and would need updating too if you rename it.

| # | Current Name | Type | Inside | Rename Safety | Suggested Name | Description (paste into Fabric) |
|---|---|---|---|---|---|---|
| 1 | **Get_Metadata_Config** | Lookup | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (used in FE_Loop_Tables' items expression) | Keep as-is | Retrieves the list of active source tables to process this run. Queries WH_Metadata.Config_Oracle.ConfigETL for up to 100 rows where IsActive = 1, returning each table's schema, name, watermark field/value, and primary key. This is the pipeline's starting activity (no dependencies); its output feeds the FE_Loop_Tables loop directly below. |
| 2 | **FE_Loop_Tables** | ForEach | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Invoke pipeline depends on it) | Keep as-is | Iterates over every active table returned by Get_Metadata_Config, up to 10 in parallel. For each table it runs Switch1, Switch2, and Switch3 in sequence to identify the table and capture its watermark value into a dedicated pipeline variable — building the full watermark set that Invoke pipeline passes downstream. |
| 2.1 | **Switch1** | Switch | FE_Loop_Tables | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Switch2 depends on it) | Keep as-is | Matches the current item's TableName against 23 known source tables and runs the matching Set-variable activity to capture that table's watermark value into a pipeline variable. Any table not in this list falls through to Wait1. |
| 2.1.1 | **Set variable2** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_ACCOUNT_SUMMARY_AAV2 | Captures the watermark value for V_ACCOUNT_SUMMARY_AAV2 — copies @item().WaterMarkValue into pipeline variable V_ACCOUNT_SUMMARY_AAV2_WaterMarkValue. |
| 2.1.2 | **Set variable3** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_FUND | Captures the watermark value for V_T_D_FUND — copies @item().WaterMarkValue into pipeline variable V_T_D_FUND_WaterMarkValue. |
| 2.1.3 | **Set variable4** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_PLAN_V2 | Captures the watermark value for V_T_D_PLAN_V2 — copies @item().WaterMarkValue into pipeline variable V_T_D_PLAN_V2_WaterMarkValue. |
| 2.1.4 | **Set variable5** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_TXN_TYPE | Captures the watermark value for V_T_D_TXN_TYPE — copies @item().WaterMarkValue into pipeline variable V_T_D_TXN_TYPE_WaterMarkValue. |
| 2.1.5 | **Set variable6** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_BRANCH | Captures the watermark value for T_D_BRANCH — copies @item().WaterMarkValue into pipeline variable T_D_BRANCH_WaterMarkValue. |
| 2.1.6 | **Set variable7** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_DEALER | Captures the watermark value for T_D_DEALER — copies @item().WaterMarkValue into pipeline variable T_D_DEALER_WaterMarkValue. |
| 2.1.7 | **Set variable8** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_REP | Captures the watermark value for T_D_REP — copies @item().WaterMarkValue into pipeline variable T_D_REP_WaterMarkValue. |
| 2.1.8 | **Set variable9** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_CSR | Captures the watermark value for T_D_CSR — copies @item().WaterMarkValue into pipeline variable T_D_CSR_WaterMarkValue. |
| 2.1.9 | **Set variable10** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_F_ACCOUNT_ENROLLMENT_AAV2 | Captures the watermark value for V_T_F_ACCOUNT_ENROLLMENT_AAV2 — copies @item().WaterMarkValue into pipeline variable V_T_F_ACCOUNT_ENROLLMENT_AAV2_WaterMarkValue. |
| 2.1.10 | **Set variable11** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_F_ACCOUNT_CLOSURE_AAV2 | Captures the watermark value for V_T_F_ACCOUNT_CLOSURE_AAV2 — copies @item().WaterMarkValue into pipeline variable V_T_F_ACCOUNT_CLOSURE_AAV2_WaterMarkValue. |
| 2.1.11 | **Set variable12** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_DW_ACCESS_CONTROL | Captures the watermark value for T_DW_ACCESS_CONTROL — copies @item().WaterMarkValue into pipeline variable T_DW_ACCESS_CONTROL_WaterMarkValue. |
| 2.1.12 | **Set variable13** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_DW_ACCESS_PARTY | Captures the watermark value for T_DW_ACCESS_PARTY — copies @item().WaterMarkValue into pipeline variable T_DW_ACCESS_PARTY_WaterMarkValue. |
| 2.1.13 | **Set variable14** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_TIME | Captures the watermark value for V_T_D_TIME — copies @item().WaterMarkValue into pipeline variable V_T_D_TIME_WaterMarkValue. |
| 2.1.14 | **Set variable15** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_DISBURSEMENT | Captures the watermark value for T_D_DISBURSEMENT — copies @item().WaterMarkValue into pipeline variable T_D_DISBURSEMENT_WaterMarkValue. |
| 2.1.15 | **Set variable16** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_DORMANCY | Captures the watermark value for T_D_DORMANCY — copies @item().WaterMarkValue into pipeline variable T_D_DORMANCY_WaterMarkValue. |
| 2.1.16 | **Set variable17** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_FUNDED | Captures the watermark value for T_D_FUNDED — copies @item().WaterMarkValue into pipeline variable T_D_FUNDED_WaterMarkValue. |
| 2.1.17 | **Set variable18** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_AGE | Captures the watermark value for T_D_AGE — copies @item().WaterMarkValue into pipeline variable T_D_AGE_WaterMarkValue. |
| 2.1.18 | **Set variable19** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_F_ORGANIZATION_MEMBER_AFFILIATION | Captures the watermark value for T_F_ORGANIZATION_MEMBER_AFFILIATION — copies @item().WaterMarkValue into pipeline variable T_F_ORGANIZATION_MEMBER_AFFILIATION_WaterMarkValue. |
| 2.1.19 | **Set variable20** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_ORGANIZATION_STATUS | Captures the watermark value for T_D_ORGANIZATION_STATUS — copies @item().WaterMarkValue into pipeline variable T_D_ORGANIZATION_STATUS_WaterMarkValue. |
| 2.1.20 | **Set variable21** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_ORGANIZATION_TYPE | Captures the watermark value for T_D_ORGANIZATION_TYPE — copies @item().WaterMarkValue into pipeline variable T_D_ORGANIZATION_TYPE_WaterMarkValue. |
| 2.1.21 | **Set variable22** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_F_ORGANIZATION_ACCOUNT | Captures the watermark value for T_F_ORGANIZATION_ACCOUNT — copies @item().WaterMarkValue into pipeline variable T_F_ORGANIZATION_ACCOUNT_WaterMarkValue. |
| 2.1.22 | **Set variable23** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_ORGANIZATION_BI | Captures the watermark value for V_T_D_ORGANIZATION_BI — copies @item().WaterMarkValue into pipeline variable V_T_D_ORGANIZATION_BI_WaterMarkValue. |
| 2.1.23 | **Set variable24** | SetVariable | Switch1 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_MEMBER_STATUS | Captures the watermark value for T_D_MEMBER_STATUS — copies @item().WaterMarkValue into pipeline variable T_D_MEMBER_STATUS_WaterMarkValue. |
| 2.1.24 | **Wait1** | Wait | Switch1 (default case) | ✅ Safe — nothing else references this activity by name | Keep as-is (standard fallback naming) | No-op fallback (1-second wait) triggered when the current table's name doesn't match any of the 23 cases in Switch1, so the loop doesn't fail on an unrecognized table. |
| 2.2 | **Switch2** | Switch | FE_Loop_Tables | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Switch3 depends on it) | Keep as-is | Runs after Switch1 succeeds. Matches the current item's TableName against 5 additional known source tables and runs the matching Set-variable activity to capture that table's watermark. Any unmatched table falls through to Wait2. |
| 2.2.1 | **Set variable 1** | SetVariable | Switch2 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_D_PROFILE_NP | Captures the watermark value for V_T_D_PROFILE_NP — copies @item().WaterMarkValue into pipeline variable V_T_D_PROFILE_NP_WaterMarkValue. |
| 2.2.2 | **Set variable 2** | SetVariable | Switch2 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_MEMBER_PERM_TYPE | Captures the watermark value for T_D_MEMBER_PERM_TYPE — copies @item().WaterMarkValue into pipeline variable T_D_MEMBER_PERM_TYPE_WaterMarkValue. |
| 2.2.3 | **Set variable 3** | SetVariable | Switch2 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_MEMBER_ROLE_TYPE | Captures the watermark value for T_D_MEMBER_ROLE_TYPE — copies @item().WaterMarkValue into pipeline variable T_D_MEMBER_ROLE_TYPE_WaterMarkValue. |
| 2.2.4 | **Set variable 4** | SetVariable | Switch2 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_F_POOL_ACCOUNT_ASSETS | Captures the watermark value for T_F_POOL_ACCOUNT_ASSETS — copies @item().WaterMarkValue into pipeline variable T_F_POOL_ACCOUNT_ASSETS_WaterMarkValue. |
| 2.2.5 | **Set variable 5** | SetVariable | Switch2 | ✅ Safe — nothing else references this activity by name | Set_Watermark_T_D_OMNIBUS_SOURCE | Captures the watermark value for T_D_OMNIBUS_SOURCE — copies @item().WaterMarkValue into pipeline variable T_D_OMNIBUS_SOURCE_WaterMarkValue. |
| 2.2.6 | **Wait2** | Wait | Switch2 (default case) | ✅ Safe — nothing else references this activity by name | Keep as-is (standard fallback naming) | No-op fallback (1-second wait) triggered when the current table's name doesn't match any case handled by Switch1 or Switch2. |
| 2.3 | **Switch3** | Switch | FE_Loop_Tables | ✅ Safe — nothing else references this activity by name | Keep as-is (consistent with Switch1/Switch2) | Runs after Switch2 succeeds. Matches the current item's TableName against the last 2 known source tables and runs the matching Set-variable activity. Any unmatched table falls through to Wait3. |
| 2.3.1 | **Set variable25** | SetVariable | Switch3 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_F_ACCOUNT_SNAPSHOT_AAV2 | Captures the watermark value for V_T_F_ACCOUNT_SNAPSHOT_AAV2 — copies @item().WaterMarkValue into pipeline variable V_T_F_ACCOUNT_SNAPSHOT_AAV2_WaterMarkValue. |
| 2.3.2 | **Set variable26** | SetVariable | Switch3 | ✅ Safe — nothing else references this activity by name | Set_Watermark_V_T_F_ACCT_TXN_AAV2 | Captures the watermark value for V_T_F_ACCT_TXN_AAV2 — copies @item().WaterMarkValue into pipeline variable V_T_F_ACCT_TXN_AAV2_WaterMarkValue. |
| 2.3.3 | **Wait3** | Wait | Switch3 (default case) | ✅ Safe — nothing else references this activity by name | Keep as-is (standard fallback naming) | No-op fallback (1-second wait) triggered when the current table's name doesn't match either case in Switch3. |
| 3 | **Update_Watermark** | SqlServerStoredProcedure | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Script1 depends on it) | Keep as-is | Writes the newly processed watermark values back to the config table by calling [Config_Oracle].[UpdateWaterMarkSP1] on WH_Metadata, so the next pipeline run only picks up rows that changed since this run. Runs after ForEach_LoadPartnerData completes successfully. |
| 4 | **Upsert_Satge_to_dbo** | SqlServerStoredProcedure | (root) | ✅ Safe — nothing else references this activity by name — currently has no dependsOn and is Inactive, so nothing points to it right now | Upsert_Stage_to_dbo (fixes the "Satge" typo) | Calls [dbo].[sp_Load_ALL_AA2_Tables] on WH_Target to bulk-load/upsert staged data into the dbo schema for all AA2 tables. Currently marked Inactive with no dependency wired in — confirm with the team whether this should be re-enabled and connected (e.g., after Invoke pipeline) or retired for good. |
| 5 | **Invoke pipeline** | InvokePipeline | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Lkp_StagetoTarget depends on it) | Invoke_01_Extract_Pipeline (matches underscore convention used elsewhere; update Lkp_StagetoTarget's dependsOn if you rename) | Calls the separate Fabric pipeline '01_Invoke_Pipeline' (workspace DEV_AGS_AA2_DI&T), passing all 30 captured watermark values as parameters, and waits for it to finish before continuing. This is the step that triggers the actual Oracle-side extraction using this run's watermark values. Runs after FE_Loop_Tables succeeds. |
| 6 | **Script1** | Script | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Script2 depends on it) | Rebuild_TXN_PLAN_MONTH_AGG (update Script2's dependsOn if you rename) | Rebuilds WH_Target.dbo.TXN_PLAN_MONTH_AGG, a monthly aggregate of transaction/plan activity (amount, shares, transaction count) grouped by time period, plan, fund, and transaction type. Creates the table on first run; otherwise merges in updated values. Logs the outcome to WH_Metadata.Log.Upsert_LOG. Runs after Update_Watermark. |
| 7 | **Lkp_StagetoTarget** | Lookup | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (ForEach_LoadPartnerData depends on it) | Keep as-is | Re-reads the active table config list (same source table as Get_Metadata_Config) to drive the partner data-copy loop below. Runs once Invoke pipeline has completed the extraction/staging step. Output feeds ForEach_LoadPartnerData. Note: confirm whether this lookup is meant to apply extra filter/query-building logic beyond the plain config read — the current query is identical to Get_Metadata_Config's. |
| 8 | **ForEach_LoadPartnerData** | ForEach | (root) | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Update_Watermark depends on it) | Keep as-is | Loops over the active table list from Lkp_StagetoTarget, up to 10 in parallel. For each table: stamps a start time, copies staged data into the target warehouse table, and logs whether that copy succeeded or failed. |
| 8.1 | **StartTime** | SetVariable | ForEach_LoadPartnerData | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Cpy_Stagetodbo depends on it) | Keep as-is | Records the current UTC time into the StartTime pipeline variable, used to time and log this iteration's copy operation. |
| 8.2 | **Cpy_Stagetodbo** | Copy | ForEach_LoadPartnerData | ⚠️ Risky — referenced by name elsewhere; only rename if you also update the references below (Success and Failure both depend on it) | Keep as-is | Copies all rows from DF_Stage.<TableName> in WH_Target and upserts them into WH_Target.dbo.<TableName>, matching on the table's primary key columns. Auto-creates the target table if it doesn't already exist. Runs after StartTime. |
| 8.3 | **Success** | SqlServerStoredProcedure | ForEach_LoadPartnerData | ✅ Safe — nothing else references this activity by name | Log_Copy_Success | Logs a successful copy to Log.usp_InsertPartnerLog — records table name, schema, start/end time, and rows copied, with Status = 'Success'. Runs when Cpy_Stagetodbo succeeds. |
| 8.4 | **Failure** | SqlServerStoredProcedure | ForEach_LoadPartnerData | ✅ Safe — nothing else references this activity by name | Log_Copy_Failure | Logs a failed copy to Log.usp_InsertPartnerLog — same fields as Success, but with Status = 'Failure' and the actual error message from Cpy_Stagetodbo (or a generic fallback message if none is available). Runs when Cpy_Stagetodbo fails. |
| 9 | **Script2** | Script | (root) | ✅ Safe — nothing else references this activity by name | Rebuild_SNAPSHOT_MONTH_AGG | Rebuilds WH_Target.dbo.SNAPSHOT_MONTH_AGG, a monthly snapshot aggregate (account count, total assets) sourced from V_T_F_ACCOUNT_SNAPSHOT_AAV2. Creates the table on first run; otherwise truncates and reloads it. Logs the outcome to the same log table as Script1. Runs after Script1. |

**49 activities total.**

## Rename order, if you decide to rename any risky ones
Rename leaf-first (Script2 → Script1 → Update_Watermark → ForEach_LoadPartnerData → Lkp_StagetoTarget → Invoke pipeline → FE_Loop_Tables → Switch3 → Switch2 → Switch1 → Get_Metadata_Config), updating each activity's own `dependsOn`/expression references as you go, so nothing points to a name that no longer exists mid-edit.
